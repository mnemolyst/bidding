extends ./layout.jade

block content
    h1 Thumb Durr Dome
    .contests
        each contest in contests
            +contest(contest)
        +contest(null)
    #new-bid-form
        input.bidder(placeholder="Bidder name")
        br
        input.amount(placeholder="Bid amount")
        br
        input.on(placeholder="Bid on")
